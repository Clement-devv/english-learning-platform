# Dependencies
node_modules/
server/node_modules/

# Environment files — never commit secrets
.env
.env.*
server/.env
server/.env.*

# Build output
dist/
build/

# Logs & editor
*.log
.vscode/
.DS_Store

# User-uploaded files — never commit these
server/uploads/content/
server/uploads/recordings/
server/uploads/homework/
# Keep branding and teacher photos tracked (small, intentional assets)

# Markdown docs
*.md
