name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  # ── Test ────────────────────────────────────────────────────────────────────
  test:
    name: Test
    runs-on: ubuntu-latest

    services:
      mongodb:
        image: mongo:7
        ports:
          - 27017:27017

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install frontend dependencies
        run: npm ci

      - name: Install server dependencies
        run: cd server && npm ci

      - name: Build frontend (smoke test)
        run: NODE_OPTIONS=--max-old-space-size=4096 npm run build
        env:
          VITE_API_URL: https://clemify.com

      - name: Run server tests
        run: cd server && npm test
        env:
          NODE_ENV: test
          JWT_SECRET: test-secret-minimum-32-characters-xxxxxxxxxxxxxxxxx
          ENCRYPTION_KEY: aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
          EMAIL_USER: test@example.com
          EMAIL_PASSWORD: testpassword
          MONGO_URI: mongodb://localhost:27017/test
          MASTER_DB_URI: mongodb://localhost:27017/test-master
          DB_BASE_URI: mongodb://localhost:27017
